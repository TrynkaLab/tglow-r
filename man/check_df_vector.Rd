% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.r
\name{check_df_vector}
\alias{check_df_vector}
\title{Check and validate grouping vector against a data frame}
\usage{
check_df_vector(df, grouping)
}
\arguments{
\item{df}{A data frame to check against}

\item{grouping}{Either a single column name in df or a character vector of length nrow(df)}
}
\value{
The grouping vector as a character if valid, NULL if grouping was NULL
}
\description{
Validates a grouping parameter that can be either a column name in a data frame
or a character vector matching the number of rows in the data frame.
}
\details{
The function handles three cases:
\enumerate{
\item grouping is NULL (returns NULL)
\item grouping is a single string matching a column name in df (returns that column)
\item grouping is a character vector of length nrow(df) (returns the vector)
}
}
\keyword{internal}
